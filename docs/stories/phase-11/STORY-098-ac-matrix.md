# STORY-098 AC Coverage Matrix

> Generated: 2026-05-04 (Task 7 close-out)

| AC | Description (summary) | Status | Evidence |
|----|----------------------|--------|----------|
| AC-1 | TBL-61 migration exists with `syslog_destinations` table, transport/format CHECK constraints, and FK to `tenants` | PASS | `migrations/20260504000001_syslog_destinations.up.sql` applies cleanly; `make db-migrate` green |
| AC-2 | List endpoint scoped by tenant; cross-tenant access returns 404 | PASS | `TestList_NoTenantContext` (403 on missing tenant ctx) + `TestSyslogDestinationStore_Delete_CrossTenant_NotFound` (store-level 404) in `internal/api/settings/log_forwarding_test.go` + `internal/store/syslog_destination_test.go` |
| AC-3 | Create/Update validates transport (udp/tcp/tls), format (rfc3164/rfc5424), TLS PEM mutual-exclusion | PASS | `TestUpsert_InvalidTransport` + `TestUpsert_InvalidFormat` + `TestUpsert_InvalidFacility` + `TestUpsert_InvalidCategory` + `TestUpsert_TLSClientCertXOR` in `internal/api/settings/log_forwarding_test.go` |
| AC-4 | SetEnabled + Delete handlers emit audit log entries via registered actions | PASS | Audit emit confirmed in `log_forwarding.go` (`emitDisabledAudit` + `emitDeleteAudit`); `TestSyslogDestinationStore_SetEnabled` + `TestSyslogDestinationStore_Delete` (store layer); 4 actions registered in 8-layer event catalog |
| AC-5 | RFC 3164 wire format: `<PRI>Mmm dd hh:mm:ss HOST argus[PID]: TYPE tenant=… severity=… TITLE` | PASS | `TestRFC3164_GoldenBytes` (emitter_test.go byte-exact assertion) + `TestIntegration_RFC3164_WireFormat` (integration_test.go field-level parse) |
| AC-6 | RFC 5424 wire format: `<PRI>1 TS HOST argus PID TYPE SD BOM MSG`; SD contains `argus@32473` + `tenant_id`; BOM = 0xEF 0xBB 0xBF | PASS | `TestRFC5424_GoldenBytes` (emitter_test.go byte-exact + BOM assert) + `TestIntegration_RFC5424_TLS_WireFormat` (integration_test.go VERSION/SD/BOM parsed from live TLS frame) |
| AC-7 | UDP transport: fire-and-forget, no retry on failure | PASS | `TestUDPTransport_RoundTrip` (transport_test.go); no retry logic in `udpTransport.Send` |
| AC-8 | TCP transport: RFC 6587 octet-counting framing; reconnect-after-error; exponential backoff sequence | PASS | `TestTCPTransport_RoundTrip_OctetCounting` + `TestTCPTransport_FramingFormat` (wire-level `<len> <body>`) + `TestTCPTransport_ReconnectAfterError` + `TestBackoff_Sequence` |
| AC-9 | TLS transport: self-signed CA accepted; no-CA rejects; hostname mismatch rejects; mutual TLS succeeds | PASS | `TestTLSTransport_SelfSignedCA_Succeeds` + `TestTLSTransport_NoCA_FailsCertVerify` + `TestTLSTransport_HostnameMismatch_Fails` + `TestTLSTransport_MutualTLS_Succeeds` (transport_test.go) |
| AC-10 | Test-connection endpoint: no DB write, no audit; returns OK or error message | PASS | `TestTest_UDPRoundTrip` + `TestTest_TCPRoundTrip` + `TestTest_UnreachableHost` in `internal/api/settings/log_forwarding_test.go` |
| AC-11 | Forwarder dispatches per category filter; 17-case subject→category mapping table | PASS | `TestForwarder_RoundTrip_OneDestination` + `TestForwarder_FilterByCategory_Excluded` + `TestCategoryForSubject` (17 cases, forwarder_test.go) + `TestIntegration_CategoryFilter_MismatchNotDelivered` (integration_test.go two-envelope positive+negative assertion) |
| AC-12 | Worker UpdateDelivery: failure records last_error; success clears it | PASS | `TestDestinationWorker_SuccessClearsLastError` (worker_test.go) + `TestIntegration_LastErrorStoredOnFailure` (integration_test.go end-to-end fakeTransport→fakeStore assertion) |
| AC-13 | SCR-198 UI: list + Add/Edit slide-panel + Test-connection + per-row actions (enable/disable/delete) | PASS | SCR-198 page shipped in T6; `web/src/pages/settings/log-forwarding/` + `LogForwardingPage` route wired in sidebar |
| AC-14 | Delivery-failure audit rate-limited to once per minute per destination | PASS | `TestDestinationWorker_AuditRateLimit_OncePerMinute` (worker_test.go): 10 failures → exactly 1 audit entry within window |
| AC-15 | Slow destination does not stall bus subscriber or other destinations | PASS | `TestForwarder_OneSlowDestinationDoesNotBlockOthers` (forwarder_test.go: fakeTransport 500ms delay) + `TestIntegration_SlowTCPDoesNotStallUDP` (integration_test.go: real TCP listener with aggressive KickConnections, 20 envelopes, UDP must receive all 20) |
| AC-16 | Zero regression: full test suite PASS at or above pre-STORY-098 count | PASS | `go test -count=1 ./...` → 4240+ tests PASS; `go test -race ./internal/notification/syslog/...` clean; `tsc` 0 errors; `vite build` green |
